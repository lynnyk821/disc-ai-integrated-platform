spring:
  datasource:
    url: jdbc:postgresql://db:5432/disc_platform
    username: disc_platform
    password: disc_platform
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: true